# ============================================
# Docker Compose Manager - Variables d'environnement
# ============================================
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# ============================================
# Backend - JWT Configuration
# ============================================
# Secret JWT pour signer les tokens d'authentification
# IMPORTANT: Utilisez une cl̩ d'au moins 32 caract̬res en production
# G̩n̩rez une cl̩ s̩curis̩e avec: openssl rand -base64 32
JWT_SECRET=CHANGE-THIS-SECRET-KEY-IN-PRODUCTION-MIN-32-CHARS

# ============================================
# Backend - Docker Configuration (optionnel)
# ============================================
# URL du daemon Docker
# Linux: unix:///var/run/docker.sock (par d̩faut)
# Windows: npipe://./pipe/docker_engine
# Remote: tcp://host:port
# DOCKER_HOST=unix:///var/run/docker.sock

# ============================================
# Frontend - API Configuration (optionnel)
# ============================================
# URL de l'API backend (utilis̩e au moment du build)
# Par d̩faut: http://localhost:5000
# VITE_API_URL=http://localhost:5000
